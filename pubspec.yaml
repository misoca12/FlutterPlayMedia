name: flutter_play_media
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Video
  video_player: ^2.2.7
  chewie: ^1.2.2

  # Audio
  just_audio: ^0.9.17
  audio_session: ^0.1.6+1
  audioplayers: ^0.20.1

  # Other
  rxdart: '>=0.26.0 <0.28.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
